<h1>Is it a square?</h1>

<p><h3>Your rectangle's length is $myRectangle.getLength()</h3></p>
<p><h3>Your rectangle's width is $myRectangle.getWidth()</h3></p>

#if ($myRectangle.isSquare())
  <p><h5>Your rectangle is a square!</h5></p>
  <h5>Surface area of a cube with those dimensions: $myCube.surfaceArea()</h5>
  <h5>Volume of a cube with those dimensions: $myCube.volume()</h5>
#else
  <p><h5>Your rectangle is not a square!</h5></p>
#end
